const PROVIDER_CATEGORIES = [
  "barber",
  "makeup",
  "esthetician",
  "hair",
  "massage",
];

const MIN_PROVIDER_CATEGORIES = 2;

const NEARBY_RADIUS_KM_DEFAULT = 25;

module.exports = {
  PROVIDER_CATEGORIES,
  MIN_PROVIDER_CATEGORIES,
  NEARBY_RADIUS_KM_DEFAULT,
};
